var StateMachine = require('./server/state-machine');

var transitionTo = StateMachine.transitionTo;

var createStateMachine = function() {
  return new StateMachine({
    
  });
}

var PlayerTurnStateMachine = function() {
  
}